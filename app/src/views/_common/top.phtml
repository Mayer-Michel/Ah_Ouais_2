<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nos Biens</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <!-- header.php -->
    <header class="header">
        <div class="logo">AhOuais.com</div>
        <nav class="navbar">
            <?php if (!$auth::isAuth()): ?>
                <a href="/sign-in" class="btn btn-primary">Connexion</a>
            <?php else: ?>
                <span class="user-greeting">Bonjour, <?php echo $auth::getUser()->getFirstname(); ?>!</span>
                <a href="/sign-out" class="btn btn-outline">Déconnexion</a>
            <?php endif ?>
        </nav>
    </header>