<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nos Biens</title>
    <link rel="stylesheet" href="/style.css"> <!-- Link to your CSS -->
</head>
<body>

<div class="container">
    <!-- Navigation -->
    <div class="navigation">
        <a href="/" class="back-link">RETOUR</a>
    </div>

    <!-- Header -->
    <div class="header">
        <h1>Nos Biens</h1>
    </div>

    <!-- Room Listings -->
    <div class="room-listing">
        <?php if( !empty($rooms) ): ?>
            <ul class="rooms">
                <?php foreach( $rooms as $room ): ?>
                    <li class="room-item">
                        <h2 class="room-title"><?php printf('%s', $room->getDescription() ) ?></h2>
                        <ul class="room-details">
                            <li>Id: <?php echo $room->getId() ?></li>
                            <li>Capacity: <?php echo $room->getCapacity() ?></li>
                            <li>Surface: <?php echo $room->getSurface() ?></li>
                            <li>Prix par jour: <?php echo $room->getPrice_day() ?></li>
                            <li>Description: <?php echo $room->getDescription() ?></li>
                        </ul>
                    </li>
                <?php endforeach; ?>
            </ul>
        <?php else: ?>
            <div class="no-rooms">Aucun bien disponible.</div>
        <?php endif; ?>
    </div>
</div>

</body>
</html>